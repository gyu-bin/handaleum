# 몰아보기 A 정리 (2026-08-07)

## 결정

- 탭 **유지**. 지도 이동형 / 풀스크린 B / 세로 피드 C는 이번 범위 밖.
- UI = **장소 페이지** (히어로 + 3열 그리드). 홈 지도와 역할 분리.
- **자동재생 제거** (의미 없는 ▶).
- 순서 = **그달 초 → 말** 시간순.

## 동작

1. `useMonthlyPhotos` → `clusterPhotos(photos, 14)`.
2. 클러스터 정렬: 장소 첫 사진 `takenAt` 오름차순.
3. 장소 안 사진: `takenAt` 오름차순 (클러스터 빌드 시 이미 정렬).
4. 가로 스와이프 = 다음 장소. 그리드 탭 = 대표/커버.

## UI (A 위계)

- 한 줄: 왼쪽 장소명 · 오른쪽 날짜 / 장수
- 히어로 → (그리드)
- 하단: ‹ › 원형 화살표 + 가운데 `n / N` (스와이프 문구 없음). 헤더 카운터 제거.
- 선택/대표 테두리 = ink

## 비범위

- 지도 카메라 연동
- 자동 넘김
- 하단 내비 IA 변경
- 풀스크린 B / 세로 피드 C
